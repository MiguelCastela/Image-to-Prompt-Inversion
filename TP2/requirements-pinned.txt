# Pinned to the versions in the working .venv. One-shot install:
#
#   pip install -r requirements-pinned.txt
#
# torch/torchvision below are plain PyPI versions. On Linux x86_64 the PyPI
# torch==2.11.0 wheel is already CUDA-enabled, so this works without any extra
# index. (The reference machine used the +cu128 build; the CUDA *minor* version
# does not affect result comparability — only the torch version 2.11.0 matters.)
#
# If `import torch; torch.cuda.is_available()` is False after install, force the
# CUDA 12.8 build explicitly instead:
#   pip install torch==2.11.0 torchvision==0.26.0 \
#       --index-url https://download.pytorch.org/whl/cu128
# triton is pulled in automatically by the Linux torch wheel (do not pin it).

torch==2.11.0
torchvision==0.26.0

# --- core model + inference stack (must match for comparable results) ---
transformers==5.10.2
tokenizers==0.22.2
diffusers==0.38.0
accelerate==1.13.0
safetensors==0.8.0rc1

# --- Qwen2.5-VL AWQ quantization runtime ---
autoawq==0.2.9
gptqmodel==7.0.0
qwen-vl-utils==0.0.14

# --- metrics ---
lpips==0.1.4

# --- numerics / io ---
numpy==2.2.6
pandas==3.0.3
scipy==1.17.1
pillow==12.2.0
tqdm==4.68.1
regex==2026.5.9
requests==2.34.2

# --- phase 1 warm-start only (NOT needed to run phase 4 with provided warmstarts) ---
open_clip_torch==3.3.0
clip-interrogator==0.6.0
